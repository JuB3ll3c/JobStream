spring:
  application:
    name: api
  jpa:
    hibernate:
      ddl-auto: create-drop

# Adzuna API configuration
adzuna:
  api:
    base-url: https://api.adzuna.com/v1/api
    app-id: test
    app-key: test
    default-country: ch